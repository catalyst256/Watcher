#Exclude conf files generated when canari package installed, not the main conf file though
*.conf
!/src/Watcher/resources/etc/*.conf

#Exclude python autosave and complied files
*.pyc
*.py~

#Exclude Watcher.mtz from src directory
/src/Watcher.mtz
